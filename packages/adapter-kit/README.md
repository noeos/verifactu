# @noeos/verifactu-adapter-kit

This is a private `0.0.0-development` P2 package shell. It contains no adapter,
certification or conformance capability. The real executable kit is delivered
only after its public contracts and fault-capable scenarios are implemented.
