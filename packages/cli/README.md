# @noeos/verifactu-cli

This is a private `0.0.0-development` P2 package shell. No `noeos-verifactu`
binary is declared yet because a placeholder command must never be mistaken for
the complete CLI contract.
