# Observed regulatory source snapshot

This immutable snapshot records a bounded 2026-09-21 observation of the BOE and
AEAT authority pages named by the source register. The HTML files are captured
representations with source-plan URL, media type, licence and content digests in
`manifest.json`. Runtime code never refreshes these URLs.

The linked AEAT developer payloads for WSDL, XSD, validation/error, hash,
signature and QR material were re-observed with ordinary TLS verification and
remain blocked by the platform certificate-chain failure or absent payload link.
No cached, browser, stale or TLS-bypassed bytes are substituted. Consequently
this is a verifiable blocked candidate, not an approved or active edition.
